# User Manual

This project provides a C++ calc_inpout library and an example application.

## Build Instructions
1. Run `make` to compile the executable.
2. Run `make test` to build and execute the test runner.

## Usage
1. Build the code with `make`.
2. Execute `./calc_inpout`.
3. The program demonstrates operations on the calc_inpout component.
