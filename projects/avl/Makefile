# Auto-generated Makefile for avl
# Generated by EvoForge — do not edit manually unless you know what you are doing.

CC      = gcc
CFLAGS  = -Wall -Wextra -std=c11 -g -I. -Itests
TARGET      = avl
SRC_ROOT    = $(wildcard *.c)
OBJ_ROOT    = $(SRC_ROOT:.c=.o)
ALL_OBJ     = $(OBJ_ROOT)
TEST_DIR    = tests
TEST_SRCS   = $(wildcard $(TEST_DIR)/*.c)
TEST_TARGET = $(TEST_DIR)/test_runner

# ── Primary build ──────────────────────────────────────────────────────────
all: $(TARGET)

$(TARGET): $(ALL_OBJ)
	$(CC) $(OBJ_ROOT) -o $@ -lm

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

# ── Test build & run ───────────────────────────────────────────────────────
test: $(TEST_TARGET)
	@echo "Running C/C++ tests..."
	./$(TEST_TARGET)
	@echo "All tests passed."

$(TEST_TARGET): $(TEST_SRCS) $(filter-out %main.o main_%.o main.o, $(OBJ_ROOT))
	@mkdir -p $(TEST_DIR)
	$(CC) $(CFLAGS) $(TEST_SRCS) $(filter-out %main.o main_%.o main.o, $(OBJ_ROOT)) -o $(TEST_TARGET) -lm

# ── Cleanup ────────────────────────────────────────────────────────────────
clean:
	rm -f $(ALL_OBJ) $(TARGET) $(TEST_TARGET) $(TEST_DIR)/*.o

.PHONY: all test clean
