# Auto-generated Makefile for avl
# Generated by EvoForge — do not edit manually unless you know what you are doing.

CXX      = g++
CXXFLAGS  = -Wall -Wextra -std=c++17 -g -I. -Itests
TARGET      = avl
SRC_ROOT    = $(wildcard *.cpp)
OBJ_ROOT    = $(SRC_ROOT:.cpp=.o)
ALL_OBJ     = $(OBJ_ROOT)
TEST_DIR    = tests
TEST_SRCS   = $(wildcard $(TEST_DIR)/*.cpp)
TEST_TARGET = $(TEST_DIR)/test_runner

# ── Primary build ──────────────────────────────────────────────────────────
all: $(TARGET)

$(TARGET): $(ALL_OBJ)
	$(CXX) $(OBJ_ROOT) -o $@ -lm

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

# ── Test build & run ───────────────────────────────────────────────────────
test: $(TEST_TARGET)
	@echo "Running C/C++ tests..."
	./$(TEST_TARGET)
	@echo "All tests passed."

$(TEST_TARGET): $(TEST_SRCS) $(filter-out %main.o main_%.o main.o CLI.o, $(OBJ_ROOT))
	@mkdir -p $(TEST_DIR)
	$(CXX) $(CXXFLAGS) $(TEST_SRCS) $(filter-out %main.o main_%.o main.o CLI.o, $(OBJ_ROOT)) -o $(TEST_TARGET) -lm

# ── Cleanup ────────────────────────────────────────────────────────────────
clean:
	rm -f $(ALL_OBJ) $(TARGET) $(TEST_TARGET) $(TEST_DIR)/*.o

.PHONY: all test clean
