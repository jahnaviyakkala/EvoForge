# Auto-generated Makefile for tictactow_game
# Generated by EvoForge — do not edit manually unless you know what you are doing.

CXX      = g++
CXXFLAGS  = -Wall -Wextra -std=c++17 -g -I. -Itests
TARGET      = tictactow_game
SRC         = $(wildcard *.cpp)
OBJ         = $(SRC:.cpp=.o)
TEST_DIR    = tests
TEST_SRCS   = $(wildcard $(TEST_DIR)/*.cpp)
TEST_TARGET = $(TEST_DIR)/test_runner

# ── Primary build ──────────────────────────────────────────────────────────
all: $(TARGET)

$(TARGET): $(OBJ)
	$(CXX) $(OBJ) -o $@ -lm

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

# ── Test build & run ───────────────────────────────────────────────────────
test: $(TEST_TARGET)
	@echo "Running C/C++ tests..."
	./$(TEST_TARGET)
	@echo "All tests passed."

$(TEST_TARGET): $(TEST_SRCS) $(OBJ)
	@mkdir -p $(TEST_DIR)
	$(CXX) $(CXXFLAGS) $(TEST_SRCS) $(filter-out main.o main_%.o, $(OBJ)) -o $(TEST_TARGET) -lm

# ── Cleanup ────────────────────────────────────────────────────────────────
clean:
	rm -f $(OBJ) $(TARGET) $(TEST_TARGET) $(TEST_DIR)/*.o

.PHONY: all test clean
