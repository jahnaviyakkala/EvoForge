# User Manual

This project provides a C++ calculaor_user library and an example application.

## Build Instructions
1. Run `make` to compile the executable.
2. Run `make test` to build and execute the test runner.

## Usage
1. Build the code with `make`.
2. Execute `./calculaor_user`.
3. The program demonstrates operations on the calculaor_user component.
