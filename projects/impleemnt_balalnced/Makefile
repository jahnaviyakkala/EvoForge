# Auto-generated Makefile for impleemnt_balalnced
# Generated by EvoForge — do not edit manually unless you know what you are doing.

CC      = gcc
CFLAGS  = -Wall -Wextra -std=c11 -g -I. -Itests
TARGET      = impleemnt_balalnced
SRC         = $(wildcard *.c)
OBJ         = $(SRC:.c=.o)
TEST_DIR    = tests
TEST_SRCS   = $(wildcard $(TEST_DIR)/*.c)
TEST_TARGET = $(TEST_DIR)/test_runner

# ── Primary build ──────────────────────────────────────────────────────────
all: $(TARGET)

$(TARGET): $(OBJ)
	$(CC) $(OBJ) -o $@ -lm

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

# ── Test build & run ───────────────────────────────────────────────────────
test: $(TEST_TARGET)
	@echo "Running C/C++ tests..."
	./$(TEST_TARGET)
	@echo "All tests passed."

$(TEST_TARGET): $(TEST_SRCS) $(OBJ)
	@mkdir -p $(TEST_DIR)
	$(CC) $(CFLAGS) $(TEST_SRCS) $(filter-out main.o main_%.o, $(OBJ)) -o $(TEST_TARGET) -lm

# ── Cleanup ────────────────────────────────────────────────────────────────
clean:
	rm -f $(OBJ) $(TARGET) $(TEST_TARGET) $(TEST_DIR)/*.o

.PHONY: all test clean
