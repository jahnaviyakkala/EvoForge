# User Manual

This project provides a C++ generate_calcualtor library and an example application.

## Build Instructions
1. Run `make` to compile the executable.
2. Run `make test` to build and execute the test runner.

## Usage
1. Build the code with `make`.
2. Execute `./generate_calcualtor`.
3. The program demonstrates operations on the generate_calcualtor component.
