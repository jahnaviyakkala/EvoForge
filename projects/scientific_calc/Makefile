# Auto-generated Makefile for scientific_calc
# Generated by EvoForge — do not edit manually unless you know what you are doing.

CC      = gcc
CFLAGS  = -Wall -Wextra -std=c11 -g -I. -Itests -Isrc
TARGET      = scientific_calc
SRC_ROOT    = $(wildcard *.c)
SRC_SRC    = $(wildcard src/*.c)
OBJ_ROOT    = $(SRC_ROOT:.c=.o)
OBJ_SRC    = $(SRC_SRC:.c=.o)
ALL_OBJ     = $(OBJ_ROOT) $(OBJ_SRC)
TEST_DIR    = tests
TEST_SRCS   = $(wildcard $(TEST_DIR)/*.c)
TEST_TARGET = $(TEST_DIR)/test_runner

# ── Primary build ──────────────────────────────────────────────────────────
all: $(TARGET)

$(TARGET): $(ALL_OBJ)
	$(CC) $(OBJ_ROOT) $(OBJ_SRC) -o $@ -lm

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

# ── Test build & run ───────────────────────────────────────────────────────
test: $(TEST_TARGET)
	@echo "Running C/C++ tests..."
	./$(TEST_TARGET)
	@echo "All tests passed."

$(TEST_TARGET): $(TEST_SRCS) $(filter-out %main.o main.o main_%.o, $(OBJ_ROOT) $(OBJ_SRC))
	@mkdir -p $(TEST_DIR)
	$(CC) $(CFLAGS) $(TEST_SRCS) $(filter-out %main.o main.o main_%.o, $(OBJ_ROOT) $(OBJ_SRC)) -o $(TEST_TARGET) -lm

# ── Cleanup ────────────────────────────────────────────────────────────────
clean:
	rm -f $(ALL_OBJ) $(TARGET) $(TEST_TARGET) $(TEST_DIR)/*.o

.PHONY: all test clean
