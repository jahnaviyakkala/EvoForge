# Auto-generated Makefile for avl_tree
# Generated by EvoForge — do not edit manually unless you know what you are doing.

CXX      = g++
CXXFLAGS  = -Wall -Wextra -std=c++17 -g -I. -Itests -Isrc
TARGET      = avl_tree
SRC_ROOT    = $(wildcard *.cpp)
SRC_SRC    = $(wildcard src/*.cpp)
OBJ_ROOT    = $(SRC_ROOT:.cpp=.o)
OBJ_SRC    = $(SRC_SRC:.cpp=.o)
ALL_OBJ     = $(OBJ_ROOT) $(OBJ_SRC)
TEST_DIR    = tests
TEST_SRCS   = $(wildcard $(TEST_DIR)/*.cpp)
TEST_TARGET = $(TEST_DIR)/test_runner

# ── Primary build ──────────────────────────────────────────────────────────
all: $(TARGET)

$(TARGET): $(ALL_OBJ)
	$(CXX) $(OBJ_ROOT) $(OBJ_SRC) -o $@ -lm

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

# ── Test build & run ───────────────────────────────────────────────────────
test: $(TEST_TARGET)
	@echo "Running C/C++ tests..."
	./$(TEST_TARGET)
	@echo "All tests passed."

$(TEST_TARGET): $(TEST_SRCS) $(filter-out %main.o main.o main_%.o, $(OBJ_ROOT) $(OBJ_SRC))
	@mkdir -p $(TEST_DIR)
	$(CXX) $(CXXFLAGS) $(TEST_SRCS) $(filter-out %main.o main.o main_%.o, $(OBJ_ROOT) $(OBJ_SRC)) -o $(TEST_TARGET) -lm

# ── Cleanup ────────────────────────────────────────────────────────────────
clean:
	rm -f $(ALL_OBJ) $(TARGET) $(TEST_TARGET) $(TEST_DIR)/*.o

.PHONY: all test clean
